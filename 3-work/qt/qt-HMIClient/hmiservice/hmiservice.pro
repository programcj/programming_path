TEMPLATE = app
CONFIG += console
CONFIG -= app_bundle
CONFIG -= qt

SOURCES += main.c

#QT += core sql xml network
#QT -= gui

#CONFIG += c++11

#TARGET = hmiservice
#CONFIG += console
#CONFIG -= app_bundle
#TEMPLATE = app

target.path=/home/root/
INSTALLS +=target
